#ifndef EQUATION_H
#define EQUATION_H
#include <iostream>
#include <string>
#include <map>
#include <stack>
#include <cctype>
#include <iostream>
#include <vector>
#include <string>
#include <regex>
#include <cmath>
#include <functional>
using namespace std;
int calculate(const std::string& s);
double evaluate(const std::string& expr);
bool evaluateExpression(const std::string& expr, std::vector<int>& numbers);
#endif // DETECT24_H#pragma once
